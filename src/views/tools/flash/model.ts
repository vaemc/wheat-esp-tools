export interface Option {
  type: string;
  fileName: string;
  title: string;
  subtitle: string;
}

export interface Firmware {
  size:string,
  check:boolean;
  path: string;
  address: string;
}

